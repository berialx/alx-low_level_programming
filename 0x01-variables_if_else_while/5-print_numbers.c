#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Print all single digits
 * Return: Always 0 (success)
 */

int main(void)
{
	int(num);

	for (num = 0; num <= 9; num++)
	{
		printf("%d", num);
	}
	printf("\n");
	return (0);
}
